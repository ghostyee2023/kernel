# Kernel · 创意种子 — 静态作品发布与投票平台 · 方案总览

> Kernel · 创意种子 —— 每一件杰作都始于一颗种子。把一个 zip 或一个 HTML 文件，变成一条可传播、可投票、可排行、会自动过期的链接。Slogan：Core. Code. Create. / 核心 · 代码 · 创造。

---

## 交付物清单

| 文件 | 内容 |
|------|------|
| `docs/01-产品方案.md` | 定位、概念模型、短码设计、9 大功能模块、权限矩阵、防刷票、生命周期、页面清单、边界与风险 |
| `docs/02-技术架构.md` | 技术选型、系统架构图、**沙箱隔离方案**、Nginx 配置、上传安全流水线、并发一致性、部署与微信登录 |
| `docs/03-数据模型与API.md` | 完整 Prisma Schema（10 张表）、REST API 全量端点、定时任务、前端目录结构 |
| `docs/04-实施路线图.md` | P0–P4 分期、人力周期、成本估算、风险清单、上线检查清单 |
| `DESIGN.md` | 设计系统规范（品牌 · 色板 / 排版 / 组件 / 布局 / 层级 / 规范 / 响应式 / 品牌表达 / 排行榜 / 活动页 / AI 提示指南） |
| `prototype/index.html` | 高保真可交互多视图原型（作品广场/详情/上传/排行榜/活动/后台/登录注册等），浅色 + 暗色双主题，零外链可离线 |

---

## 核心方案决策

**1. 一个作品 = 一个文件夹 = 一串短码 = 一条链接**
8 位 Base58 短码（去掉易混字符），既是目录名也是子域名：`k7Rm2xQz.demo-sandbox.com`。

**2. 沙箱隔离是这个产品的生死线**
用户上传任意 HTML 等于允许陌生人在你的域名上执行 JS。方案强制：**独立注册域名**承载用户内容（不是主站子域）、沙箱域零 Cookie、CSP 加 `form-action 'none'` 防钓鱼、详情页 iframe 不给 `allow-same-origin`。这一条如果妥协，整个平台会变成 XSS 分发器。

**3. 三种投稿方式，覆盖所有场景**
单 HTML 文件 / ZIP 打包（自动解压+入口识别）/ 仅提供外链（新标签页跳转）。

**4. 可见性三态 + 管理员全权限带审计**
公开 / 仅链接 / 私密。管理员可查看私密内容，但每次访问写审计日志——有权限也要留痕。

**5. 投票六层防护**
微信身份唯一 → Redis 频率限流 → 设备指纹 → 停留时长校验 → 后台可疑票聚合与批量作废 → 高风险自动触发验证码。

**6. 两套排行榜**
活动榜用纯票数（评奖公平），发现榜用带时间衰减的热度算法（避免早鸟垄断首页）。

**7. 生命周期：默认 90 天，但留了三道缓冲**
到期前 7 天 / 1 天微信提醒 → 到期归档进回收站保留 30 天可恢复 → 获奖作品与榜单前十自动豁免。

**8. 设计语言**
参考 Linear（排版精度）+ Vercel（近白画布、克制黑墨）+ Framer（展示张力）。品牌 **Kernel · 创意种子**：三色品牌梯度靛 `#4C5BD4` / 青 `#12B8B0` / 品红 `#E0489A`（早期第四色 Amber 已废弃）。近白画布 `#FFFFFF`/`#FAFAFB`，1px 发丝边框定义结构，强调色单页面积不超过 5%——**平台是画框，作品才是主角**。渐变文字仅 Hero 标题一处。

---

## 原型覆盖页面

作品广场（Hero 中文标题「每一件杰作，都始于一颗种子」+ 品牌渐变 + Slogan 胶囊 + 4 列卡片网格，卡片**整体可点**进入详情、已移除「全屏运行」按钮）· 作品详情（浏览器框预览 + 投票 + 二维码分享 + 有效期管理，左侧竖线已去、说明集中右侧面板）· 上传发布（三种模式 + 拖拽区 + 安全校验反馈 + 表单 + 发布成功页）· 排行榜（前三名**奖杯** + 榜单列表 + 进入页**烟花庆祝**）· 活动广场与详情（「参加活动 · 让作品被看见」标题、无筛选器 / 无 eyebrow、详情 CTA 合并、无状态提示框、活动即标签）· 后台管理（侧边栏 + KPI + 作品表格 + 清理策略 + 投票风控，已移除标签管理等冗余模块）· 移动端抽屉导航。

原型内所有交互可点：视图切换、投票（含数字跳动与取消）、开关、主题切换、Toast 反馈。底部原型调试浮层已移除，正式以真实导航为准。

---

## 建议的下一步

1. **先定域名**：主域 + 沙箱域两个独立注册域，同步启动备案（国内场景 20 天起）
2. **确认微信资质**：开放平台网站应用需企业主体认证，¥300/年；同时准备手机号登录兜底
3. **P0 两周跑通骨架**：上传 → 短码 → 沙箱访问 → 自动过期，先不做投票
4. 如需，我可以基于 `DESIGN.md` 直接生成 Next.js 项目脚手架与组件库代码

**周期估算**：单人全栈 7–9 周，两人并行 5 周。年成本约 ¥5,200–8,500（自建）。
